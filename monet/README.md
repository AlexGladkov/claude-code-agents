# Monet agents

Глобальные агенты Monet (копия рабочей папки `~/.monet/agents`).
Monet грузит из `~/.monet/agents` — эта папка версионирует их в git.
Синк: `cp ~/.monet/agents/*.yaml AI/agents/monet/agents/` (или наоборот).
